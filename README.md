Install dependencies 

server dependencies
```sh
npm install or yarn 
```
client dependencies
```sh
npm run client-install or yarn client-install
```

to start dev server:
```sh
npm run dev or yarn dev
```